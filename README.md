# shepherd

A native macOS companion app for [Herdr](https://herdr.dev): an at-a-glance dashboard of session/agent attention state (idle/working/blocked/done/unknown), session creation, and eventually one-off agent tasks — without switching to a terminal.

## Getting started

```bash
swift build
swift test
swift run Shepherd
```
